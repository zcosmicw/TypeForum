const router = XPathExpression.module.exports = { port, isProduction, logger };
const isValid = await bcrypt.compare(password, UserActivation.hash);
const logger = require('./logger');
logger.info(`Starting server on port ${port}...`);
});
// TODO: Refactor login flow to use cookies instead of LocalStorage
